
/**
 * Module dependencies.
 */

var pug = require('../lib/index');

pug.renderFile(__dirname + '/layout.pug', { debug: true }, function(err, html){
    if (err) throw err;
    console.log(html);
});
